- if(isSuccess)
 script
  alert('그룹이 만들어졌습니다.')
  location.href = '/join/#{groupName}/#{character}';
- else if(groupName === '')
 script
  alert('그룹을 만들지 못했습니다.');
  location.href = '/enter';
- else
 script
 alert('이미 존재하는 그룹입니다.');
 location.href = '/enter';
