- if(isSuccess)
 script(type='text/javascript')
  function enterRoom(roomName){
   location.href='/join/'+roomName;
  }; 
 
 h2 채팅방 목록
 p #{nickName}님 환영합니다.
 p 원하는 채팅방이 없으면 채팅방을 만드세요.
   form(action='/makeroom', method='POST')
    label(for='roomname') 방제
    input(type='text', id='roomname', name='roomname')
    input(type='submit', value='방만들기')
 - if(roomList && roomList.length)
  ul
   each room in roomList
    li= room.name
     input(type='button', value='입장', onclick='enterRoom(\'' + room.name + '\')');
- else
 - if(nickName == '')
  script(type='text/javascript')
   alert('닉네임을 입력하세요.')
   history.go(-1)
 - else
  scritp(type='text/javascript')
   alert('사용중인 닉네임입니다.')
   history.go(-1)