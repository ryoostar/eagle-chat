- if(isSuccess != false)
 script(type='text/javascript')
  function enterRoom(groupName){
   location.href='/join/'+groupName;
  }; 
 
 h2 채팅방 목록
 p #{nickName}님 환영합니다.
 p 원하는 채팅방이 없으면 채팅방을 만드세요.
   form(action='/makeGroup', method='POST')
    label(for='groupName') 방제
    input(type='text', id='groupName', name='groupName')
    input(type='submit', value='그룹 만들기')
 - if(groupList && groupList.length)
  ul
   each group in groupList
    li= group.name
     input(type='button', value='입장', onclick='enterRoom(\'' + group.name + '\')');
- else
 - if(nickName == '')
  script(type='text/javascript')
   alert('닉네임을 입력하세요.')
   history.go(-1)
 - else
  scritp(type='text/javascript')
   alert('사용중인 닉네임입니다.')
   history.go(-1)